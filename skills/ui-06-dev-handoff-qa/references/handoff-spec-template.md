# Handoff Spec Template

# <Feature> — Dev Handoff

**Figma:** link (DevMode enabled) **ClickUp:** task link **A11y report:** link
**Status:** ready for dev / in build / in QA / signed off

## Screens
| Screen | Frame name | Breakpoints | States delivered |
|--------|-----------|-------------|------------------|

## Layout
Grid per breakpoint, spacing tokens used, safe areas. Responsive behavior between breakpoints (what stretches, wraps, hides).

## Components
| Element | Library component + variant | Props/overrides | Token exceptions |
|---------|-----------------------------

## Interaction states
Per element: state → trigger → visual change (token names) → animation (duration ms, easing, property).

## Accessibility requirements (from UI 05)
Tab order per screen, accessible labels, announcements for dynamic content, contrast-verified pairs.

## Edge cases
Link to edge case doc; each case: trigger → expected behavior → copy.

## Copy table
| Key | EN (approved) | MM (approved) | Notes |
|-----|---------------|---------------|-------|

## Out of scope
Explicitly listed to prevent scope creep in build.
